using static System.Math;
public static class math{
	static int Main(){
		double sqrt2 = Sqrt(2.0);
		double expPi = Exp(PI);
		double piPowerE = Pow(PI,E);
		System.Console.Write($"The square root of 2 is {sqrt2} \ne to the power pi is {expPi} \npi to the power e is {piPowerE} \n");
		return 0;
	}
}
