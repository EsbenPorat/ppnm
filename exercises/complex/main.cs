using static System.Math;
using static System.Console;
using static cmath;
using static complex;
public static class calculations{
	public static int Main(){
	Write("The value of sqrt(-1) is , manually calculated to i \n")
	Write("The value of sqrt(i) is ,  manually calculated to 0.7071 + 0.7071*i \n")
	Write("The value of e^i is ,  manually calculated to 0.540 + 0.841*i \n")
	Write("The value of e^(i*pi) is ,  manually calculated to -1 \n")
	Write("The value of i^(-i) is ,  manually calculated to 4.180 \n")
	Write("The value of ln(i) is ,  manually calculated to 1.571*i \n")
	Write("The value of sin(i*pi) is ,  manually calculated to 11.549*i \n")
	return 0;
	}
}
